# V12 Historical Experiments

These scripts produced the reset, continuity, projection-consistency, shared
similarity and streaming audits used to select Movie3R-Single V12.0.

The `v14_*` filenames are intentionally retained for cache and report
compatibility. The supported top-level entrypoints are `../run.py` and
`../viewer.py`; files here are research and audit entrypoints.
