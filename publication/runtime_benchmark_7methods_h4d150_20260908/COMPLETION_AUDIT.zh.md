# 七方法 H4D-CS150 时间测试完成审计

审计日期：2026-09-08。

## 范围

- 方法：Human3R、Shot3R、PromptHMR (SPEC)、OnlineHMR、TRACE、TRAM、JOSH。
- 输入：hugging、grappling2、sword2、mma4、mma5 五条固定 Harmony4D 多镜头序列。
- 每条输入 150 帧（两个依次出现的 75 帧单目镜头），每种方法共 750 帧。
- 硬件：每个预测阶段使用单张 NVIDIA L20；不使用多 GPU 推理。
- 计时：包含必要预测阶段的进程启动、模型加载及方法所需的检测、分割、跟踪、相机恢复、优化和人体恢复，直到原生结果写盘；多阶段官方流程取必要阶段时间之和。排除数据下载/数据集解压、评测、论文格式转换和渲染。
- PromptHMR 与 TRACE 的保留记录还包含逐样本的轻量硬链接输入整理；该步骤不进行图像解码或重新编码，因此其结果相对保守。

## 直接验证结果

- `aggregate.py` 已重新执行成功，并按唯一 `case_id` 而非不同仓库中的 manifest 行号解析记录。
- 共有 35 条方法—序列记录：7 种方法各 5 条，每条均为 150 帧。
- Human3R、Shot3R、PromptHMR、OnlineHMR、TRACE 和 TRAM 的状态均为 `success`；JOSH 的五条记录均为 `audited`，且全部必要阶段完成、返回码为 0。
- 35 条记录的原生输出均存在；聚合表中保存的记录 SHA-256 与当前文件逐一一致。
- 所有记录绑定相同的五个不可变 `case_id`；保留的 TRACE 输入与规范 RGB 输入在全部 5×150 帧上逐文件字节一致。
- GPU 记录或运行设备索引均解析为 NVIDIA L20。
- 论文内的 `runtime_summary.csv` 与重新聚合的正式汇总文件 SHA-256 相同：`c43556995fd4343d9e83718b1d863c29fe84c8e54a83f55602c89d589e201f63`。

## 聚合口径

- 每种方法的 FPS 为微平均吞吐率：`750 / 五条序列总墙钟时间`。
- `s / 150 frames` 为五条序列的均值，误差项为总体标准差。
- 采用微平均 FPS，避免直接平均逐序列 FPS 对较快序列赋予过高权重。

## 论文交付验证

- v052 正文 Table 1 报告六种具有同协议精度结果的方法及 H4D FPS。
- 补充材料 Table 5 报告全部七种方法的时间、FPS、时间访问方式及相机/场景输出范围。
- 摘要报告 Shot3R 的 0.894 FPS，并将 2.7--6.0 倍吞吐率严格限定为所评测的离线与半在线流程。
- Human3R 与 TRACE 的更高吞吐率在正文和表格中明确保留。
- Overleaf ZIP 已在全新目录中独立解压并从 `main.tex` 编译：33 页、US Letter，无未定义引用、未定义文献或 overfull box。
- 两份可编辑 PPTX 的作者与最后修改者元数据为空。

最终文件 SHA-256：

- PDF：`3d6f1d1ca5748ec69791433b1a7716b2f05f91979d9937498a5641a5b4df2d55`
- Overleaf ZIP：`6c1e8fe41dc72646bff30ea34f811de68c8ff93224959e32bac2e83325757399`
