# EgoHumans 可视化结果索引（2026-09-16）

## 推荐结果

最终推荐版是：

`Shot3R_EgoHumans_cam03_ClearPeople_30frames_20260916`

- 素材：EgoHumans `003_legoassemble`。
- 视角：固定 `cam03`。
- 背景源帧：`91–149`，步长 2，共 30 张。
- 人物源帧：`91 / 111 / 139`。
- 背景：3,402,779 个融合点，2.5 mm 体素。
- 人物不透明度：`30% / 45% / 90%`。
- 保留原因：cam03 场景覆盖全面，人物位于左右和后方，中央积木遮挡较少；
  单相机重建避免了跨视角未对齐造成的杂乱。
- 完整说明：该目录下的 `RESULT_CARD_中文.md`。
- 素材预览：该目录下的 `input_contact_sheet.jpg` 和
  `selected_human_frames.jpg`。
- 默认端口：`8104`。

## 保留的历史正式实验

以下目录不是本轮临时探索，仍完整保留：

- `Shot3R_EgoHumans_10frames_20260907`：多方法 5+5 帧正式对比。
- `Shot3R_EgoHumans_9frames_3shots_20260909`：cam03/cam06/cam04 三镜头实验。

## 保留的选材依据与公共代码

- `Shot3R_EgoHumans_AlternativeViewSearch_20260916`：保留五个相机的全序列
  contact sheet、候选清单和抽帧脚本；单张重复候选图可从原始数据再生成。
- `Shot3R_EgoHumans_DenseBackground_80frames_8views_20260916`：只保留当前
  最终版依赖的通用准备、重建、融合和 viewer 代码，不保留被否定的八视角输出。
- `Shot3R_EgoHumans_cam03_DenseBackground_30frames_20260916`：只保留当前
  最终版依赖的单相机背景和人物导出代码，以及旧方案的小型元数据。

## 已删除的中间结果

| 方案 | 原端口 | 删除原因 |
|---|---:|---|
| 8 相机、80 帧背景融合 | 8100 | 跨视角累计后杂乱、对齐不稳定；仅保留公共代码 |
| cam03/cam05/cam06、90 帧融合 | 8101 | 多视角未完全对齐，后续被单 cam03 方案替代 |
| cam03 源帧 241–299 | 8102 | 背景较好，但人物靠近中央并遮挡积木；仅保留支持代码和元数据 |
| cam04 源帧 151–209 | 8103 | 对面视角探索，最终用户选择继续使用覆盖更全面的 cam03 |

端口 `8100–8103` 已关闭；最终版 `8104` 保持运行。
