# Teaser 模板优化

1. 保留上传模板，使用独立版本目录输出 PPTX、SVG、PDF 和预览。
2. 从原始 EgoHumans 九帧文件替换上方代表帧（261、291、321）及底部九帧。
3. 用此前 0°→177° 推理展示的原始预测网格和点云渲染中部，选择四个真实时刻（251、261、311、331）；同一身份始终同色，前三个时刻淡化，末时刻清晰。
4. 场景使用预测深度、颜色、相机和人体掩码，适量采样并采用剖切视图，保留处理参数与原始三维资产。人体不重塑、不插值、不平移造出运动。
5. 该已有推理是两视角结果，照片时间线是三视角输入示例。文件说明及图中标注区分两者，不把现有两视角输出写成新三视角推理结果。
6. 保持文字、相机示意、箭头为矢量；点云和每人每时刻渲染独立成层。检查 PPTX 和 SVG 渲染、图层数量、照片来源，再打包。

状态：以上六项均已完成（2026-09-06）。

交付目录：`SHOT3R_refined_editable/`。原模板保留在 `source/`，所有替换内容进入独立版本。PPTX 已用 LibreOffice 实际打开并导出 PDF，SVG/PDF 已渲染检查。PPTX 与 SVG 各有 25 张独立图片：上方 3 张、底部 9 张、中部 13 层。PPTX 内 136 个对象编号均唯一，所有图片关系可解析。

保留的后续事项：如果希望中部也表示完整的 0°→99°→177° 三镜头执行结果，需要对这一新的三镜头时间线单独推理；本版已明确注明中部沿用既有 0°→177° 推理，不改写实验结论。
