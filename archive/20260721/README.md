# Experiment Archive 2026-07-21

This directory contains completed or rejected boundary-alignment probes that
are no longer part of the active workspace. Files were moved here rather than
deleted so prior results remain recoverable.

The current retained methods and run commands are documented in:

`docs/movie3r/ACTIVE_BOUNDARY_ALIGNMENT.md`

Archive layout:

- `scripts/`: inactive untracked experiment entry points.
- `docs/`: inactive experiment reports.
- `output/archive/20260721/`: generated experiment outputs and logs.

Tracked repository files and runtime dependencies of the retained methods were
left in their original locations.
