# v030 Figure and Table Plan

## Main paper

1. **Figure 1, real extreme-view transition.** Real EgoHumans RGB and
   method-native outputs; it illustrates the task and does not replace a
   quantitative comparison. Editable SVG and provenance are retained.
2. **Figure 2, method diagram.** Shows the causal information flow: read-only
   history provides gauge evidence, while only clean-reset recurrence is
   propagated. Editable SVG is the source of the publication PDF.
3. **Table 1, paired multi-person results.** Strict Human3R and Bridge3R on the
   same RGB/evaluator within each of EgoBody, EgoHumans, and Harmony4D.
4. **Table 2, viewpoint interaction.** Dataset-equal all/extreme summaries and
   a 20,000-draw recording/capture-cluster interaction interval.
5. **Figure 3, EgoHumans angle strata.** Data-generated vector curve with all
   four bins and uncertainty; the adverse small-view W result remains visible.
6. **Table 3, mechanism/input evidence.** Separates oracle diagnosis from the
   deployable causal row, association denominators, and detector timing.

## Supplement

- Complete raw and paired viewpoint tables plus an editable vector summary.
- Full detector timing table, including recall and late-trigger columns.
- Complete AIST++ single- and repeated-cut results.
- Per-dataset uncertainty, local/body, boundary, temporal, association, and
  executable-public-method tables.
- MVHuman weak-texture failure audit and PromptHMR availability result.

No generated image is used as scientific evidence. Every quantitative plot or
reconstruction panel is linked to a retained artifact; image-generation tools
may not invent results.
