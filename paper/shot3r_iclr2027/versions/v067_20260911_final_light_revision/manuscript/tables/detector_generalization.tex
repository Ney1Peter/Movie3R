\begin{table}[t]
  \centering
  \caption{First-positive detector timing. MVHuman is a held-out weak-texture
  stress test, not part of the three-dataset primary aggregate. Recall at the
  annotated event is 1.0 for both inputs; FP/1k counts raw off-boundary
  positives per 1000 scored transitions.}
  \label{tab:detector-generalization}
  \scriptsize
  \resizebox{\columnwidth}{!}{%
    \input{artifacts/detector_generalization/detector_generalization_main}}
\end{table}
