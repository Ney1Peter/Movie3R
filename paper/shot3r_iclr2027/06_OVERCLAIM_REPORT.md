# v029 Overclaim Report

## Remove or narrow

- `exclusively owns`: replace with “is the only recurrent state propagated after the event”.
- `gains are most pronounced`: replace with the measured dataset-equal interaction and its interval.
- `These results establish`: replace with “The evidence supports” and retain the exact task scope.
- Any implication that individual correction tokens are independently necessary: unsupported without independently trained checkpoints.
- Any implication of scene-reconstruction improvement: outside the evaluated target.
- Any implication of real-time or general runtime superiority: unsupported by the retained one-clip measurement.

## Keep with qualifications

- Causality: keep because future frames, GT boundary and GT identity are excluded at runtime; define the permitted sigma-algebra explicitly.
- Extreme-view robustness: keep at the dataset-equal interaction level; show all-view and extreme subsets plus uncertainty.
- Association effectiveness: keep with both conditional and continuation denominators and zero runtime abstention.
- Parameter efficiency: keep as an accounting statement (2.79%), not as proof that each subcomponent is necessary.
